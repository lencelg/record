# Java
## string in java
* LSD Radix sort 
* MSD Radix sort 
* 3-way string quicksort 
    linear time and in-place , but not stable , genearl 1.39nlogN performance for random data use extra space of logN + w because you have to use recursion which relates to stack.
* suffix array and linearithmic suffix sort 
